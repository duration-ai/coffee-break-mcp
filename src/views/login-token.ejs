<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stats - Coffee Break MCP</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="page">
    <main class="stack">
      <h1 class="title">Stats</h1>
      <p class="subtitle">Enter your token to load stats.</p>

      <% if (error) { %>
        <div class="alert"><%= error %></div>
      <% } %>

      <form class="form" method="POST" action="/stats">
        <input
          type="text"
          name="token"
          placeholder="token"
          value="<%= token %>"
          required
        >
        <button class="button" type="submit">View stats</button>
      </form>

      <div class="row">
        <a class="button" href="/auth/register">Generate token</a>
        <a class="button" href="/">Back</a>
      </div>
    </main>
  </div>
</body>
</html>
